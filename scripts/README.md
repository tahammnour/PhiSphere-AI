# Scripts

Utility scripts for development and presentation assets.

## Presentation generator

- `generate_presentation.py`: Creates `PhiSphere_AI_Presentation.pptx`

Run:

```bash
python3 scripts/generate_presentation.py
```

Output:

- `PhiSphere_AI_Presentation.pptx` (repo root)

## Notes

- Update repository/demo links inside the generated deck before final submission if placeholders are still present.

